import assert from 'node:assert/strict';
import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';

const gameRoot = new URL('../', import.meta.url);
const read = path => readFile(fileURLToPath(new URL(path, gameRoot)), 'utf8');
const main = await read('src/main.js');
const index = await read('index.html');
const viewport = await read('src/systems/mobile-viewport-hardening.js');
const owner = await read('src/systems/mobile-input-single-owner-v1.js');

assert.equal((index.match(/src\/systems\/mobile-input-single-owner-v1\.js/g) || []).length, 1, 'single-owner module must be loaded exactly once');
assert.ok(index.indexOf('src="/src/main.js"') < index.indexOf('src="/src/systems/mobile-input-single-owner-v1.js"'), 'single-owner must load after main boot');
assert.match(index, /data-mobile-action/);
assert.match(main, /Mobile input ownership lives exclusively/);
assert.match(owner, /MOBILE INPUT SINGLE OWNER V9/);
assert.match(owner, /cloneNode\(true\)/);
assert.match(owner, /replaceWith\(clone\)/);
assert.match(owner, /seen\.has\(action\)/, 'owner must deduplicate actions added by legacy boot code');
assert.match(owner, /pointerup/);
assert.match(owner, /pointercancel/);
assert.match(owner, /lostpointercapture/);
assert.match(owner, /visibilitychange/);
assert.match(owner, /pagehide/);
assert.match(owner, /ACTION_KEYS = Object\.freeze/);
assert.match(owner, /actionPointers/);
assert.match(owner, /pointerActions/);
assert.match(owner, /pointers\.size === 0/);
assert.match(owner, /mobileControlsOwner/);
assert.match(owner, /single-owner-v9/);
assert.match(owner, /if \(pointerId !== null\) return;/, 'joystick must reject a second pointer while one is active');
assert.match(owner, /const end = event => \{/);
assert.match(owner, /if \(event && event\.pointerId !== pointerId\) return;/, 'joystick release must only clear its owning pointer');
assert.doesNotMatch(viewport, /mobile-controls-bridge-v2\.js/, 'viewport hardening must not install a second mobile movement owner');
console.log('Mobile input single-owner V9 contract: PASS');
