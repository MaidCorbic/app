# Clean mobile map integration

Uses the existing web map renderer as the single source of truth. Mobile changes are limited to responsive viewport sizing and live mission-state synchronization. No alternate map, fake route data, gameplay logic, keyboard controls, or mobile controls are introduced.