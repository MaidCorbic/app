# Mobile map parity

The existing mission map renderer remains the source of truth. The mobile layer only adapts the existing map viewport for campaign levels; it does not add input ownership or replace mission data.