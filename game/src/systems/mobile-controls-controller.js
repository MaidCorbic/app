const ACTION_KEYS={jump:' ',fire:'e',sword:'q',build1:'1',gadget1:'3'};
const ACTION_LABELS={jump:'JUMP — SPACE',fire:'FIRE — E',sword:'SWORD — Q',dash:'DASH — SHIFT',build1:'BUILD — 1',gadget1:'GEAR — 3'};
const emitKey=(key,type)=>document.dispatchEvent(new KeyboardEvent(type,{key,code:key===' '?'Space':key.length===1?`Key${key.toUpperCase()}`:key,bubbles:true,cancelable:true}));
const emitDash=(source='mobile-controller')=>window.dispatchEvent(new CustomEvent('relay:new-gameplay-dash',{detail:{source}}));
if(!document.getElementById('relay-mobile-controls-controller-style')){const style=document.createElement('style');style.id='relay-mobile-controls-controller-style';style.textContent=`body.is-touch .mobile-controls{--relay-touch-size:clamp(44px,11.5vw,54px);left:max(8px,env(safe-area-inset-left,0px) + 6px)!important;right:max(8px,env(safe-area-inset-right,0px) + 6px)!important;bottom:max(10px,env(safe-area-inset-bottom,0px) + 8px)!important;align-items:flex-end!important;gap:8px!important;visibility:visible;opacity:1;pointer-events:auto;touch-action:none;z-index:2147481000!important}body.is-touch .mobile-joystick{flex:0 0 clamp(68px,18vw,82px)!important;width:clamp(68px,18vw,82px)!important;height:clamp(68px,18vw,82px)!important;pointer-events:auto!important;touch-action:none}body.is-touch .mobile-joystick-thumb{width:38px!important;height:38px!important;margin:-19px 0 0 -19px!important}body.is-touch .mobile-actions{flex:0 1 auto!important;display:grid!important;grid-template-columns:repeat(3,var(--relay-touch-size))!important;grid-auto-rows:var(--relay-touch-size)!important;gap:4px!important;width:calc(var(--relay-touch-size) * 3 + 8px)!important;max-width:calc(100vw - 92px)!important;pointer-events:auto!important;touch-action:none}body.is-touch .mobile-controls button{width:var(--relay-touch-size)!important;height:var(--relay-touch-size)!important;min-width:0!important;min-height:0!important;margin:0!important;padding:3px!important;line-height:1!important;font-size:clamp(7px,1.8vw,9px)!important;pointer-events:auto!important;touch-action:manipulation!important;-webkit-tap-highlight-color:transparent;user-select:none}#relayMobileSettings{position:fixed!important;z-index:2147481001!important;right:max(10px,env(safe-area-inset-right,0px) + 8px)!important;bottom:max(86px,env(safe-area-inset-bottom,0px) + 82px)!important;width:52px!important;height:38px!important;pointer-events:auto!important;touch-action:manipulation!important}body.relay-cinematic-active #relayMobileSettings,body.relay-cinematic-active .mobile-controls{visibility:hidden!important;opacity:0!important;pointer-events:none!important}@media(max-width:380px){body.is-touch .mobile-controls{--relay-touch-size:40px;gap:6px!important;left:6px!important;right:6px!important;bottom:max(8px,env(safe-area-inset-bottom,0px) + 6px)!important}body.is-touch .mobile-joystick{flex-basis:64px!important;width:64px!important;height:64px!important}}@media(orientation:landscape){body.is-touch .mobile-actions{grid-template-columns:repeat(6,var(--relay-touch-size))!important;grid-auto-rows:var(--relay-touch-size)!important}}`;document.head.appendChild(style)}
function isTouchDevice(){return navigator.maxTouchPoints>0||'ontouchstart'in window||matchMedia('(pointer: coarse)').matches||matchMedia('(hover: none)').matches}
function findPauseButton(){return document.getElementById('pauseBtn')||document.getElementById('pause')||document.querySelector('[data-action="pause"],[data-pause-button]')}
function findSettingsTab(){return document.querySelector('#pauseMenu [data-tab="settings"],#pauseMenu [data-section="settings"],#pauseMenu [data-settings-tab]')}
function openMobileSettings(){const pause=document.getElementById('pauseMenu'),pauseButton=findPauseButton();if(!pause||document.body.classList.contains('relay-cinematic-active'))return false;const open=()=>{const tab=findSettingsTab();if(tab&&!tab.disabled){tab.click();return true}return false};const isOpen=!pause.classList.contains('hidden')&&!pause.hidden&&pause.getAttribute('aria-hidden')!=='true';if(!isOpen&&pauseButton)pauseButton.click();if(open())return true;requestAnimationFrame(()=>{if(!open())setTimeout(open,80)});return true}
function ensureSettingsButton(){if(!isTouchDevice()||document.getElementById('relayMobileSettings'))return;const game=document.getElementById('game');if(!game)return;const button=document.createElement('button');button.id='relayMobileSettings';button.type='button';button.setAttribute('aria-label','Open mobile settings');button.textContent='SETTINGS';button.addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();openMobileSettings()},{passive:false});game.appendChild(button)}
function bindControls(controls){if(!controls||!isTouchDevice()||controls.dataset.mobileControlsOwner==='controller')return false;const cleanControls=controls.cloneNode(true);cleanControls.dataset.mobileControlsOwner='controller';controls.replaceWith(cleanControls);cleanControls.querySelector('[data-mobile-action="build2"]')?.remove();cleanControls.querySelector('[data-mobile-action="gadget2"]')?.remove();const joystick=cleanControls.querySelector('[data-mobile-joystick]'),thumb=joystick?.querySelector('.mobile-joystick-thumb'),buttons=[...cleanControls.querySelectorAll('[data-mobile-action]')];buttons.forEach(button=>{const action=button.dataset.mobileAction,key=ACTION_KEYS[action];button.setAttribute('aria-label',ACTION_LABELS[action]||action.toUpperCase());button.addEventListener('pointerdown',e=>{e.preventDefault();e.stopPropagation();if(document.body.classList.contains('relay-cinematic-active'))return;if(action==='dash')emitDash();else if(key){emitKey(key,'keydown');window.setTimeout(()=>emitKey(key,'keyup'),110)}else return;button.classList.add('is-active');window.setTimeout(()=>button.classList.remove('is-active'),110)},{passive:false});button.addEventListener('contextmenu',e=>e.preventDefault())});if(!joystick||!thumb)return true;let pointerId=null,direction=null;const setDirection=next=>{if(next===direction)return;if(direction==='left')emitKey('a','keyup');if(direction==='right')emitKey('d','keyup');direction=next;if(next==='left')emitKey('a','keydown');if(next==='right')emitKey('d','keydown')};const resetJoystick=()=>{setDirection(null);pointerId=null;joystick.classList.remove('is-active');thumb.style.transform='translate(0,0)'};const updateJoystick=(x,y)=>{const r=joystick.getBoundingClientRect(),dx=x-r.left-r.width/2,dy=y-r.top-r.height/2,d=Math.min(Math.hypot(dx,dy),Math.max(24,r.width*.42)),a=Math.atan2(dy,dx);thumb.style.transform=`translate(${(Math.cos(a)*d).toFixed(1)}px,${(Math.sin(a)*d).toFixed(1)}px)`;setDirection(Math.abs(dx)<Math.max(8,r.width*.12)?null:dx<0?'left':'right')};joystick.addEventListener('pointerdown',e=>{if(document.body.classList.contains('relay-cinematic-active'))return;e.preventDefault();e.stopPropagation();pointerId=e.pointerId;joystick.setPointerCapture?.(pointerId);joystick.classList.add('is-active');updateJoystick(e.clientX,e.clientY)},{passive:false});joystick.addEventListener('pointermove',e=>{if(e.pointerId!==pointerId)return;e.preventDefault();updateJoystick(e.clientX,e.clientY)},{passive:false});const end=e=>{if(e&&e.pointerId!==pointerId)return;resetJoystick()};joystick.addEventListener('pointerup',end);joystick.addEventListener('pointercancel',end);joystick.addEventListener('lostpointercapture',resetJoystick);window.addEventListener('pointerup',end,{passive:true});window.addEventListener('pointercancel',end,{passive:true});window.addEventListener('blur',resetJoystick,{passive:true});document.addEventListener('visibilitychange',()=>{if(document.hidden)resetJoystick()},{passive:true});return true}
function install(){if(!isTouchDevice())return;ensureSettingsButton();const existing=document.querySelector('.mobile-controls');if(existing?.dataset.mobileControlsOwner==='controller')return;if(bindControls(existing))window.__relayMobileControlsController={version:'2.5.0',root:document.querySelector('.mobile-controls'),rebind:()=>bindControls(document.querySelector('.mobile-controls')),openSettings:openMobileSettings,reset:()=>document.querySelector('[data-mobile-joystick]')?.dispatchEvent(new Event('lostpointercapture'))}}
function observe(){install();if(window.__relayMobileControlsObserver)return;window.__relayMobileControlsObserver=new MutationObserver(()=>install());window.__relayMobileControlsObserver.observe(document.body,{childList:true,subtree:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',observe,{once:true});else observe();