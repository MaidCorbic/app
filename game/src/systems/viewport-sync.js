// Single mobile viewport synchronization owner.
const mobile = () => !!(window.matchMedia?.('(pointer: coarse)').matches || Number(navigator.maxTouchPoints||0)>0 || Math.min(window.innerWidth||0,window.innerHeight||0)<=900);
const size = () => { const vv=window.visualViewport; return {width:Math.round(vv?.width||window.innerWidth||document.documentElement.clientWidth||0),height:Math.round(vv?.height||window.innerHeight||document.documentElement.clientHeight||0)}; };
let timer=0,frame=0,applying=false,last='';
const apply = reason => { frame=0; if(!mobile()) return; const {width,height}=size(); if(!width||!height)return; const key=`${width}x${height}`; if(key===last&&reason!=='orientationchange')return; last=key; const root=document.documentElement; root.style.setProperty('--relay-viewport-width',`${width}px`); root.style.setProperty('--relay-viewport-height',`${height}px`); root.dataset.relayOrientation=width>=height?'landscape':'portrait'; applying=true; window.dispatchEvent(new Event('resize')); applying=false; document.dispatchEvent(new CustomEvent('relay:viewport-settled',{detail:{reason,width,height,orientation:width>=height?'landscape':'portrait'}})); };
const schedule = reason => { clearTimeout(timer); timer=setTimeout(()=>{if(frame)cancelAnimationFrame(frame); frame=requestAnimationFrame(()=>apply(reason));},60); };
window.addEventListener('orientationchange',()=>schedule('orientationchange'),{passive:true}); window.addEventListener('resize',()=>{if(!applying)schedule('resize')},{passive:true}); window.visualViewport?.addEventListener('resize',()=>schedule('visualViewport.resize'),{passive:true});
if(document.readyState==='loading') document.addEventListener('DOMContentLoaded',()=>schedule('initial'),{once:true}); else schedule('initial');