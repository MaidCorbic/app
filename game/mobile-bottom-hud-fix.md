# Mobile HUD fix

The mobile pause/settings controls now use a dedicated fixed HUD instead of the gameplay header. Legacy-bound mobile action nodes are replaced before the single input owner attaches listeners.
